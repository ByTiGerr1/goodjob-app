// lib/theme/app_colors.dart

import 'package:flutter/material.dart';

// Colores primarios de la marca
class AppColors {
  static const Color primary = Color(0xFF7B0997); // Morado principal
  static const Color accent = Color(0xFFFFD900);  // Amarillo Brillante

  // Colores funcionales/de estado
  static const Color success = Color(0xFF4CAF50); // Verde (Confirmado/Éxito)
  static const Color error = Color(0xFFF44336);   // Rojo (Rechazado/Error)
  static const Color warning = Color(0xFFFF9800); // Naranja (Advertencia/Pendiente)
  static const Color activo = Color(0xFF00897B);
  static const Color alertColor = Color(0xFFD32F2F);
  static const Color paidColor = Color(0xFF00C853); // Color para indicar "Pagado"
  static const Color neutralColor = Colors.black54;
  static const Color securityColor = Color(0xFF00796B); // Color para datos sensibles/bancarios
  static const Color disabledColor = Color(0xFF9E9E9E);
  static const Color rejectColor = Color(0xFFD32F2F); // Rojo para indicar "Rechazado"
  static const Color pendingColor = Color(0xFFFFA000); // Naranja para indicar "Pendiente"
  static const Color cancelColor = Color(0xFF757575); // Gris para cancelar
  static const Color timelineColor = Color(0xFFB39DDB); // Púrpura más claro para la línea
  static const Color confirmadoPorUsuario = Color(0xFF00897B,); // VERDE AZULADO (Confirmado por Usuario - Compromiso)





  // Colores de texto y fondo
  static const Color textDark = Color(0xFF212121); // Negro oscuro para texto principal
  static const Color backgroundLight = Color(0xFFFAFAFA);
}
